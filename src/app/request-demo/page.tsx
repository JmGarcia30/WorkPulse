import Link from 'next/link';
import { requestDemoAction } from './actions';
export default async function RequestDemoPage({ searchParams }: { searchParams: Promise<{ sent?: string; error?: string }> }) { const state = await searchParams; return <main className="wp-public-shell min-h-screen px-5 py-16"><div className="mx-auto max-w-2xl"><Link href="/" className="font-bold">← WorkPulse</Link><div className="mt-12 border-t-4 border-[var(--wp-accent)] bg-white p-8"><p className="wp-eyebrow">Request demo</p><h1 className="wp-page-title mt-3">Tell us about your workforce operation.</h1>{state.sent && <p role="status" className="mt-5 border-l-4 border-emerald-600 bg-emerald-50 p-3 text-sm text-emerald-800">Thank you. Your request has been received.</p>}{state.error && <p role="alert" className="wp-alert-danger mt-5">Check the form and try again.</p>}<form action={requestDemoAction} className="mt-8 grid gap-5 sm:grid-cols-2"><input className="hidden" tabIndex={-1} autoComplete="off" name="website" /><label className="wp-label">Name<input required minLength={2} maxLength={100} className="wp-input mt-2" name="name" /></label><label className="wp-label">Work email<input required className="wp-input mt-2" type="email" name="email" /></label><label className="wp-label sm:col-span-2">Organization<input required className="wp-input mt-2" name="organization" /></label><label className="wp-label sm:col-span-2">What should we focus on?<textarea required minLength={10} maxLength={2000} className="wp-input mt-2 min-h-28" name="message" /></label><button className="wp-button-primary sm:col-span-2" type="submit">Send demo request</button></form></div></div></main>; }
